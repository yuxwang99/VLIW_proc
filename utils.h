#ifndef UTILS_H    
#define UTILS_H

#include <fstream>
#include <iostream>
#include <ctype.h>

using namespace std;
bool isNumeric(std::string const &str);
string findRegister(std::string const &str);

#endif